stages:
  - lint
  - test
  - build

variables:
  POSTGRES_DB: sipdok_test
  POSTGRES_USER: runner
  POSTGRES_PASSWORD: runner_password
  POSTGRES_HOST_AUTH_METHOD: trust

lint-php:
  stage: lint
  image: php:8.3-cli-alpine
  script:
    - apk add --no-cache git unzip
    - cd backend
    - curl -sS https://getcomposer.org/installer | php
    - php composer.phar install --no-interaction --prefer-dist --optimize-autoloader
    - ./vendor/bin/php-cs-fixer fix --dry-run --diff

# Verifies that no component calls an API method that does not exist. This
# defect shipped three times (projectsApi.get, exportsApi.export*, the whole
# reviewsApi action set) because a missing method only throws on click, so the
# build stayed green while the button did nothing.
check-frontend-api:
  stage: lint
  image: node:20-alpine
  script:
    - cd frontend
    - npm ci
    - npm run check:api

test-backend:
  stage: test
  image: php:8.3-cli-alpine
  services:
    - postgres:15-alpine
  variables:
    DB_CONNECTION: pgsql
    DB_HOST: postgres
    DB_PORT: 5432
    DB_DATABASE: sipdok_test
    DB_USERNAME: runner
    DB_PASSWORD: runner_password
  before_script:
    - apk add --no-cache postgresql-dev unzip git
    - docker-php-ext-install pdo pdo_pgsql
    - cd backend
    - curl -sS https://getcomposer.org/installer | php
    - php composer.phar install --no-interaction --prefer-dist --optimize-autoloader
    - cp .env.testing .env
    - php artisan key:generate --env=testing
  script:
    - php artisan test

build-frontend:
  stage: build
  image: node:20-alpine
  script:
    - cd frontend
    - npm ci
    - npm run build
  artifacts:
    paths:
      - frontend/dist/
    expire_in: 1 week
